# 📝 pdfToWord
Convert any PDF into editable Word (.docx) format using OCR (Tesseract).

A simple Python project that converts **PDF files to editable Word (.docx)** documents using **OCR (Optical Character Recognition)** via `pytesseract` and `pdf2image`.

---

## 🚀 Features
- Converts scanned or image-based PDFs into editable Word files  
- Uses **Tesseract OCR** for text extraction  
- Works on Windows, macOS, and Linux (with Poppler installed)

---

## ⚙️ Requirements
- Python 3.x
- [Poppler for Windows](https://github.com/oschwartz10612/poppler-windows)
- [Tesseract OCR](https://github.com/tesseract-ocr/tesseract)

Install the dependencies first:
```bash
pip install -r requirements.txt
